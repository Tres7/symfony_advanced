{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer une tâche</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Créer</button>
    {{ form_end(form) }}
    <a href="{{ path('index_task') }}">Retour</a>
{% endblock %}
